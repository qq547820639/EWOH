# EWOH Factory Site Reports

Place VAL-62 verified site-readiness reports here as `*.json` files matching
`ewoh:///site-readiness/v1`. The work console exposes them through
`GET /api/work/site-readiness`. Files named `*.example.json` are contract
examples, not real factory evidence.

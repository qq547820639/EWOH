# CodeBuddy 本地项目状态(记忆/自动化等,不提交)
.codebuddy/

# macOS
.DS_Store
.AppleDouble/
.Spotlight-V100/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 运行时数据库（SQLite）
*.db
*.db-shm
*.db-wal

# 凭据与环境（仅样例 .env.example 入库，真实 .env 禁止提交）
.env

# 临时文件
*.tmp
*.log
.tmp/

# 工作目录
.workbuddy/
.codebuddy/

# 本地测试与临时产物（不入库）
.playwright-cli/
tmp/
.pytest_cache/
.ruff_cache/
test-results/
ewoh-spark-app/playwright-report/
# 飞书集成运行时配置（含真实 base_token，不入库；请以 feishu-config.example.json 为模板本地创建）
ewoh-feishu-app/feishu-config.json
# CI/本地生成的测试计数报告（单一事实源运行时生成，永不入库）
ewoh-spark-app/jest.results.json
ewoh-spark-app/client/jest.results.json
output/ewoh-storage.json
# 单一事实源 evidence manifest 为运行时/CI 派生产物：evaluatedCommitSha 与
# environmentFingerprint 随 HEAD/工具链变化（自指失效），且本地与 CI 指纹不同，
# 故不入库（FR-T7/FR-T8）；不可变证据由 CI artifact 保存，仓库仅存规范与生成器。
output/evidence-manifest.json

# Node.js
node_modules/

# 构建产物
dist/
build/
.turbo/
*.tsbuildinfo

# EWOH 开发依赖清单
# 运行时为纯标准库零依赖；以下仅用于开发、CI 与可选集成测试。
# 安装：python -m pip install -r requirements-dev.txt

ruff      # 代码风格、import 排序与静态检查（替代 flake8/isort 的统一工具）
bandit    # Python AST 静态安全扫描，CI 中以 -ll（低噪音）级别运行
pytest    # 可选集成测试框架；主测试仍使用标准库 unittest，pytest 仅在需要时启用

# 安全策略

本文件描述 EWOH 受控试点系统的安全策略，包括漏洞报告流程、平台安全边界、
人员授权与隐私原则、数据保留策略、密钥与凭据管理以及部署安全要求。

## 报告漏洞流程

- 请勿在公开 issue 中提交安全漏洞相关内容。
- 通过私下渠道（安全负责人邮箱 / 内部工单）报告漏洞，并包含：
  - 受影响版本与组件；
  - 复现步骤与最小用例；
  - 影响评估与建议修复方向。
- 收到报告后我们将在 5 个工作日内确认接收，并按严重程度排期修复与披露。
- 在修复发布前，请勿公开披露漏洞细节。

## 安全边界声明

EWOH 平台是**只读监督与风险分析平台**，不参与设备实时安全控制。以下能力
**永久保留在设备控制器本地**，平台不得写入、下发或绕过：

- 急停（emergency stop / e-stop）
- 限扭设定（joint torque limit set）
- 关节实时控制（joint real-time control）
- 助力实时闭环（assist closed-loop control）
- 限速放宽（speed limit relaxation）
- 异常退出保护（abnormal exit protection）
- 设备失联安全态（device-loss safe state）
- 绕过本地安全检查的调试指令（debug commands bypassing local safety checks）

平台下发的仅限于**任务建议、风险提示、配置参数（非实时控制类）**，最终执行
决策权与安全责任在设备控制器与现场操作人员。CI 中以 grep 守护对这些关键
符号进行最低限度检查，但正式审查仍需人工（见 `.github/workflows/security.yml`）。

### 多租户隔离边界（Batch 11 补充）

数据库隔离采用「RLS 白名单 + 应用层 org 过滤」双策略：

- **RLS 覆盖表**（50+ 业务表）：`ewoh_org_visible(org_id)` 策略强制行级隔离
  （standalone_001_schema.sql 动态白名单）。
- **全局共享表（非 RLS）**：以下调度 V2 运行时表**按设计全局共享**，不按 org 隔离：
  - `ewoh_world_state_snapshot`：快照版本全局唯一（WS-YYYYMMDD-NNNN），跨 org 引用
  - `ewoh_outbox`：SSE 事件全局序列（前端按组织订阅）
  - `ewoh_assignment_event` / `ewoh_replan_trigger` / `ewoh_scheduling_run` 等：
    含 `org_id` 列但靠**应用层过滤**隔离（如 `listRuns` 按 actor.primaryOrgId 过滤）。
- **风险提示**：调度 V2 读路径必须显式 org 过滤（新增查询以 `toOrgContext` 的
  primaryOrgId 为条件）；补 RLS 覆盖需重新设计快照/事件 org 边界（见
  `docs/decisions/OPEN-DECISIONS.md`）。

## 支持的版本策略

| 版本    | 状态         | 说明                                   |
|---------|--------------|----------------------------------------|
| 0.6.x   | 受控试点     | 当前试点版本，接受安全修复             |
| < 0.6   | 不再支持     | 演示原型，仅作存档，不接受安全修复     |

后续生产版本（1.0+）发布后，将在此表中声明各版本的安全维护窗口。

## 人员授权与隐私原则

- **最小必要**：仅采集完成风险分析与任务协同所需的最小数据。
- **默认不采集**以下敏感数据，除非获得明确的书面授权并完成 DPA 评估：
  - 姓名、身份证号等强标识身份信息；
  - 长期精确轨迹（>证据窗口的高频位置序列）；
  - 视频影像；
  - 生理数据（心率、肌电等）。
- 人员标识在平台内以**脱敏编号**为主，姓名仅在授权范围内可见。
- 角色-权限矩阵见 `delivery/04_安全合规/RBAC_matrix.csv`，遵循最小权限与职责分离。
- 离场人员数据按保留策略及时降级或清除。

## 数据保留策略

| 数据类别             | 保留窗口       | 处置方式                       |
|----------------------|----------------|--------------------------------|
| 高频原始遥测         | 7 - 30 天      | 超期降采样或清除               |
| 事件证据片段         | 与事件同周期   | 事件关闭后按策略归档/清除      |
| 审计日志             | ≥ 180 天       | 仅追加，不可篡改，按合规要求留存 |
| 任务与协同记录       | 试点周期内     | 试点结束按 DPA 处置            |
| 人员授权与同意记录   | 授权有效期 +1 年 | 离场后留存以备审计             |

保留窗口可通过 `EWOH_DATA_RETENTION_DAYS` 等环境变量调整，但不得低于
合规下限。备份介质同等适用保留策略。

## 密钥与凭据管理

- `.env` 不得提交到仓库，仅 `.env.example` 作为样例入库。
- 生产环境的 `EWOH_JWT_SECRET`、`EWOH_DB_URL`、TLS 私钥等敏感凭据须通过
  密钥管理系统注入（环境变量 / secrets），禁止硬编码于镜像或代码。
- 密钥须定期轮换，轮换记录入审计日志。
- 本地账号（`EWOH_AUTH_BACKEND=local`）仅用于开发/联调，生产环境必须切换为
  `oidc` 或 `customer`（客户侧 SSO）。

## 部署安全要求

- **默认不开放公网**：平台服务监听内网地址，外部访问须经 edge-gateway（nginx）
  反向代理并终结 TLS。
- **四区部署**：详见 `docs/deployment/README.md`，分别为设备控制区、采集适配区、
  平台服务区、用户接入区，区间按最小必要原则进行网络隔离与访问控制。
- **DB 不直接对普通用户网开放**：postgres 仅接入 `ewoh-internal` 内部网络，
  普通用户网不可直达。
- **TLS 强制**：生产环境所有外部接入必须经 TLS。
- **审计可追溯**：所有写操作、导出、登录/鉴权事件入审计日志。
- **离线安全态**：设备失联超过 `EWOH_OFFLINE_AFTER_SEC` 即标记为失联安全态，
  平台不再基于其数据下发建议，直至恢复心跳并完成一致性校验。
